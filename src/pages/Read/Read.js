import BibleHeader from './BibleHeader';
import './Read.css';

const Read = () => {

    return (
        <section className='Read'>
            <BibleHeader />
        </section>
    );
};

export default Read;